@model IEnumerable<PYFP.Models.EstadoProducto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EstadoProductoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EstadoProductoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EstadoProductoId })
        </td>
    </tr>
}

</table>
